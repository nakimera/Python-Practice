#get 2 integers from the user
dividend,divisor=eval(input('Please enter two numbers to divide :'))
#if possible divide them and report the results
if divisor!=0:
    print(dividend,'/',divisor,'=',dividend/divisor)
